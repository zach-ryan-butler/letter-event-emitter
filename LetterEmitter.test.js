const LetterEmitter = require('./LetterEmitter');

describe('LetterEmitter', () => {
    beforeEach(() => {
        let LetterEmitter = new LetterEmitter();
    });

    it('splits a string and emits an event for each letter', done => {
        
    });
});
